<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/setting"
    android:visibility="gone"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_main"
    android:fitsSystemWindows="true">


    <ImageView
        android:id="@+id/img_set"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="24dp"
        android:src="@drawable/ic_settings"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/atv_pp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="36dp"
        android:background="@drawable/bg_setting_item"
        android:drawableStart="@drawable/ic_item_pp"
        android:drawableEnd="@drawable/icon_right"
        android:drawablePadding="8dp"
        android:padding="18dp"
        android:text="Privacy Policy"
        android:textColor="#FF2FC336"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@+id/img_set" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/atv_share"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_setting_item"
        android:drawableStart="@drawable/ic_item_share"
        android:drawableEnd="@drawable/icon_right"
        android:drawablePadding="8dp"
        android:padding="18dp"
        android:text="Share with Friends"
        android:textColor="#FF2FC336"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@+id/atv_pp" />
</androidx.constraintlayout.widget.ConstraintLayout>